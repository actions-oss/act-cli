name: Hello World Docker Action Workflow

on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Hello World Docker Action
        uses: ./docker-action-host-env
        with:
          who-to-greet: "GitHub"